////import { db } from "@/lib/prisma";

//export const getRestaurantBySlug = async (slug: string) => {
//const restaurant = await db.restaurant.findUnique({ where: { slug } }); //usado para chamar o bando de dados
//return restaurant;
//};
